<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        .left {
            width: 50%;
            height: 100vh;
        }

        .right {
            width: 50%;
            height: 100vh;
            background-color: #4724B2;
        }

        form input[type="email"],
        form input[type="password"] {
            width: 100%;
            margin-top: 1rem;
            height: 2.3rem;
        }

        .login {
            background-color: #4724B2;
        }

        .blackColor {
            color: #313131;
        }
    </style>
</head>

<body>

    <div class="loginRegisterParent d-flex">
        <div class="left d-flex align-items-center justify-content-center">
            <div class="leftParent w-50">
                <h2 class="fs-1 text-center">Giriş Yap</h2>
                <% if (flashSuccess.length > 0) { %>
                    <div class="alert alert-success">
                      <%= flashSuccess %>
                    </div>
                    <% } %>
                    <% if (flashError.length > 0) { %>
                    <div class="alert alert-warning">
                      <%= flashError %>
                    </div>
                    <% } %>
                <form action="/pt/login" method="POST">
                    <input placeholder="E-mail Giriniz" type="email" id="email" name="email" required>
                    <input placeholder="Şifre Giriniz" type="password" id="password" name="password" required>
                    <input type="submit" value="Giriş Yap"
                        class="login border-0 mt-3 text-white block w-100 py-2 rounded-2">
                </form>
                <div class="text-center mt-2">
                    <a href="/pt/forgot-password" class="blackColor">Şifremi Unuttum ?</a>
                </div>
            </div>
        </div>
        <div class="right d-flex align-items-center justify-content-center">
            <div class="rightParent w-50 text-white">
                <h2 class="fs-1 text-center">Kayıt Ol</h2>
                <div class="text-center mt-3">
                    <a href="/pt/register"
                        class="d-block bg-white rounded-2 text-decoration-none py-2  w-100 blackColor registerBtn">
                        Kayıt Ol
                    </a>
                </div>
            </div>
        </div>
    </div>

</body>

</html>